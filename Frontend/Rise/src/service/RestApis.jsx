
const RestApis = () => {
    
    const BASE_URL = `http://localhost:8080`;
    const DEVELOPER = `/dev`;
    const VERSION = `/v1`;
    
    //USER
    const FORGET_PASS = `${BASE_URL}${DEVELOPER}${VERSION}/user/forgetPass`;
}

export default RestApis;